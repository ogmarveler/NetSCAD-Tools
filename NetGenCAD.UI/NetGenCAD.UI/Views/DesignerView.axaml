<UserControl
    x:Class="NetGenCAD.UI.Views.DesignerView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NetGenCAD.UI.ViewModels;assembly=NetGenCAD.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="using:Markdown.Avalonia"
    xmlns:views="using:NetGenCAD.UI.Views"
    xmlns:vm="using:NetGenCAD.UI.ViewModels"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    x:CompileBindings="True"
    x:DataType="vm:DesignerViewModel"
    Background="{DynamicResource WindowBackground}"
    mc:Ignorable="d">

    <StackPanel
        MaxWidth="1920"
        Margin="20"
        HorizontalAlignment="Stretch"
        Background="{DynamicResource CardBackground}">
        <md:MarkdownScrollViewer
            x:Name="MarkdownView"
            Margin="20"
            HorizontalAlignment="Stretch"
            TextBlock.Foreground="{DynamicResource CardForeground}"
            TextBlock.TextAlignment="Justify"
            TextBlock.TextWrapping="Wrap" />
    </StackPanel>
</UserControl>